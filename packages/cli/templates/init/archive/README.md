# Archived Init Templates

This directory keeps historical bootstrap materials that are no longer consumed
by the current `harness-codex init` implementation.

Archived reference sets:

- `codex-pir/`
- `superpowers-partial/`

They are retained only for historical comparison and migration context.
